

services:
  app:
    container_name: web_portfolio
    build:
      context: .
      target: dev   # usamos la etapa dev del Dockerfile
    volumes:
      - type: bind
        source: .
        target: /app
    environment:
      - NODE_ENV=development
    command: npm run dev
    ports:
      - "3000:3000"
